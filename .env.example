# Example environment variables for Docker / local development
PORT=8010
# Paths are relative to the backend working directory inside the container (/app/backend)
DYNAMIC_CONFIG_PATH=../dynamic/dynamic.yml
TRAEFIK_CONFIG_PATH=../config/traefik.yml
